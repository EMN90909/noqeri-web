import React,{useState} from 'react'
import {Page} from '../components.jsx'
const stages=['Discover','Resolve','Lock','Download','Verify']
export function Registry(){const[a,setA]=useState(4);return <Page kicker="REGISTRY / VERIFIED" title="The Noqeri registry, built for deterministic packages." intro="Find packages, inspect metadata, lock versions and download reproducible project surfaces."><div className="registry-status"><span><i/>Registry online</span><span>Lockfile ready</span><span>Verified downloads</span></div><section className="registry-feature"><div><small>FEATURED OFFICIAL PACKAGE</small><h2>noqeri/json</h2><p>Inspectable package metadata with deterministic download paths and compiler-checked entrypoints.</p><a className="cta" href="/registry/noqeri/json/1.0.0/download.nqpkg">Download package <span>↓</span></a></div><label><span>FIND A PACKAGE</span><input placeholder="Find a package"/></label></section><section className="verify-path"><div className="verify-line" aria-hidden="true"/>{stages.map((s,i)=><button key={s} className={i<=a?'on':''} onClick={()=>setA(i)}><b>{String(i+1).padStart(2,'0')}</b><span>{s}</span></button>)}</section><div className="notice">Verification path: discover package metadata → resolve a version → lock its identity → download the archive → verify its SHA-256 identity. The interaction visualizes the real registry contract; it does not fake a download state.</div></Page>}
