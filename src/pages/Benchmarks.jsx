import React from 'react'
import { Card, Grid, Page } from '../components.jsx'
export function Benchmarks(){return <Page kicker="07 / BENCHMARKS" title="Measure the compiler and the programs separately." intro="Frontend speed is already fast enough for interactive work. Native runtime comparisons will only be published when equivalent optimization and execution harnesses exist."><Grid cols={3}><Card index="CHECK" title="≈ 3.43 ms"><p>501-function / 504-line generated source on the Ubuntu CI runner.</p></Card><Card index="RUN" title="≈ 3.43 ms"><p>Compile plus reference interpreter for the same generated workload.</p></Card><Card index="BUILD" title="≈ 13.96 ms"><p>Compile plus native assembly generation on the same runner.</p></Card></Grid><div className="notice">These are compiler-throughput measurements, not claims that generated Noqeri machine code is faster than mature C, Rust or Zig toolchains.</div></Page>}
