import React from 'react'
import {motion} from 'motion/react'
import {Page} from '../components.jsx'
const rows=[['check',3.43,'501-function / 504-line generated source'],['run',3.43,'compile + reference interpreter'],['build',13.96,'compile + native assembly generation']]
export function Benchmarks(){const max=Math.max(...rows.map(x=>x[1]));return <Page kicker="BENCHMARKS / CONTEXT" title="Benchmarks with context." intro="Performance numbers matter only when the setup is visible. Noqeri benchmark pages show what was measured, how it was run and what changed."><div className="benchmark-rig"><span>source</span><i/> <span>build</span><i/> <span>run</span><i/> <span>compare</span></div><section className="benchmark-chart"><div className="benchmark-scan"/>{rows.map(([name,value,note])=><div className="benchmark-row" key={name}><b>{name}</b><div><motion.span initial={{scaleX:0}} whileInView={{scaleX:value/max}} viewport={{once:true}} transition={{duration:.9,ease:[.22,1,.36,1]}}/></div><strong>≈ {value.toFixed(2)} ms</strong><small>{note}</small></div>)}</section><div className="benchmark-meta"><span>source</span><span>target</span><span>time</span><span>runs</span><span>median</span><span>notes</span></div><div className="notice">Benchmarks are development signals, not permanent product promises. These values are compiler-throughput measurements from the existing project, not claims that generated Noqeri machine code is faster than mature C, Rust or Zig toolchains.</div></Page>}
