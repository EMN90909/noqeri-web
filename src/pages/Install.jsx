import React from 'react'
import { Code, Page } from '../components.jsx'
export function Install(){return <Page kicker="08 / INSTALL" title="Build the bootstrap. Inspect everything." intro="Noqeri is currently a source-built compiler bootstrap. Official binary distribution and a toolchain manager belong to the next ecosystem stage."><Code>{`git clone https://github.com/EMN90909/Noqeri.git\ncd Noqeri\ncmake -S . -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build --parallel\n./build/noqeri --version`}</Code><div className="command-grid"><div><strong>Check</strong><code>noqeri check app.nqr</code></div><div><strong>Run</strong><code>noqeri run app.nqr</code></div><div><strong>Build</strong><code>noqeri build app.nqr</code></div><div><strong>Test</strong><code>noqeri test tests</code></div></div></Page>}
