import React,{useState} from 'react'
import {Page} from '../components.jsx'
const nodes=[['Language','Readable syntax and explicit boundaries.'],['Compiler','Parser, semantics, NIR and targets.'],['Runtime','Host contracts and target execution.'],['Web','Portable browser and server objects.'],['Data','Typed embedded data surfaces.'],['Registry','Immutable package distribution.'],['Packages','Reusable capabilities without syntax bloat.'],['Docs','The inspectable manual.'],['Playground','A browser syntax desk.'],['Community','People extending the system.']]
export function Ecosystem(){const[a,setA]=useState(0);return <Page kicker="ECOSYSTEM / MAP" title="One language is only the start." intro="Noqeri becomes useful when the language, tools, registry, docs, playground and community move together."><section className="ecosystem-map"><svg viewBox="0 0 720 500" role="img" aria-label="Interactive map of the Noqeri ecosystem"><circle className="eco-core" cx="360" cy="250" r="58"/><text x="360" y="255" textAnchor="middle">NOQERI</text>{nodes.map((n,i)=>{const ang=(Math.PI*2*i/nodes.length)-Math.PI/2,x=360+Math.cos(ang)*190,y=250+Math.sin(ang)*170;return <g key={n[0]} className={a===i?'active':''} onMouseEnter={()=>setA(i)} onFocus={()=>setA(i)} tabIndex="0"><line x1="360" y1="250" x2={x} y2={y}/><circle cx={x} cy={y} r="34"/><text x={x} y={y+4} textAnchor="middle">{n[0]}</text></g>})}</svg><aside><small>HOVER THE MAP</small><h2>{nodes[a][0]}</h2><p>{nodes[a][1]}</p><div className="eco-index">{String(a+1).padStart(2,'0')} / {nodes.length}</div></aside></section><div className="ecosystem-stories">{[['A language surface','Readable code stays the common path.'],['A toolchain','Compiler stages remain visible and inspectable.'],['A web path','Platform capability lives in modules and hosts.'],['A package economy','Distribution becomes deterministic, not mysterious.'],['A place to learn','Docs and community turn implementation into shared knowledge.']].map((x,i)=><article key={x[0]}><small>0{i+1}</small><h2>{x[0]}</h2><p>{x[1]}</p></article>)}</div></Page>}
