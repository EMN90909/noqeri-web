export const docGroups=['Start','Language','Safety + systems','Web + server','Data + NoqeriDB','Packages + security','Tooling + deployment']
const s=(title,paragraphs,points=[])=>({title,paragraphs:Array.isArray(paragraphs)?paragraphs:[paragraphs],points})
const d=(slug,group,title,summary,sections,code='',note='')=>({slug,group,title,summary,sections,code,note})
export const docs=[
d('getting-started','Start','Getting started','Install Noqeri 1.0, check a source file, run it and choose a target only when you need one.',[s('Small loop','Noqeri keeps the first development loop short: write .nqr, run `noqeri check`, then use `run`, `web`, or `build` according to where the program should execute.'),s('Artifacts','Source is `.nqr`. Portable web output is `.nqo`. NoqeriDB scripts are `.nqd`, while durable embedded data files are `.nqdb`.')],'noqeri check src/main.nqr\nnoqeri run src/main.nqr\nnoqeri web src/client.nqr build/client.nqo'),
d('installation','Start','Installation','Build the compiler with CMake on Linux, macOS or Windows and verify the installation contract.',[s('Bootstrap','The 1.0 compiler bootstrap is C++17 and produces the `noqeri` CLI plus the public ABI library.'),s('Verify','Use version, targets and doctor immediately after building so target and package contracts are visible rather than assumed.')],'cmake -S . -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build --parallel\n./build/noqeri version\n./build/noqeri doctor .'),
d('first-project','Start','First project','Create a project with a readable manifest and deterministic lock file.',[s('Create','`noqeri new` creates `project.nqr`, source and test directories, and a lock file.'),s('Manifest','The manifest is language-like rather than TOML/JSON ceremony. It names the entry point, target, registry and dependencies.')],'noqeri new hello\ncd hello\n../noqeri check'),
d('project-manifest','Start','project.nqr','Understand the manifest grammar, profiles, targets and dependency sources.',[s('Fields','A project defines name, version, edition, entry, profile, target and registry. Dependencies can come from registry, Git or local paths.'),s('Integrity','Remote dependencies need immutable SHA-256 identities before the lock is considered complete.')],'project {\n  name: "app"\n  version: "1.0.0"\n  edition: "2026"\n  entry: "src/main.nqr"\n}'),
d('cli','Start','CLI reference','Use a compact command surface for checking, NIR inspection, native builds, web objects, data scripts and tooling.',[s('Compiler','`check`, `nir`, `run`, `build`, `web`, `assemble`, and `link` expose compiler stages.'),s('Project','`new`, `manifest`, `lock`, `format`, `test`, `audit`, `doctor`, `targets`, `db`, and `lsp` cover project work.')],'noqeri web src/ui.nqr dist/ui.nqo\nnoqeri db schema.nqd data/app.nqdb'),
d('language-tour','Language','Language tour','Read ordinary Noqeri before learning any systems-specific feature.',[s('Core syntax','The common surface is deliberately conventional: `let`, `const`, `function`, `record`, `if`, `while`, `return`, imports and modules.'),s('Progressive power','Pointers, atomics, volatile access, intrinsics and assembly exist, but ordinary application code does not need them.')],'function greet(name: string): string {\n  return "Hello, " + name\n}'),
d('types','Language','Type system','Use fixed-width numbers, application types, pointers, arrays, slices and explicit conversions.',[s('Static checking','Noqeri checks assignments and calls before NIR lowering. Numeric width and signedness matter.'),s('Conversions','Safe widening can be implicit. Narrowing and signedness-changing conversions require `as`.')],'let small: u8 = 42\nlet wide: u32 = small\nlet again: u8 = wide as u8'),
d('variables','Language','Variables + constants','Bind values with inference or annotations and make immutability visible.',[s('Bindings','`let` creates a mutable binding. `const` cannot be reassigned.'),s('Scope','Bindings are lexical, so inner blocks can keep temporary values local.')],'const limit: u32 = 64\nlet count: u32 = 0\ncount = count + 1'),
d('functions','Language','Functions','Declare typed parameters and returns, then cross host or linker boundaries explicitly with extern/export.',[s('Functions','Calls are checked for arity and assignability before lowering.'),s('Boundaries','`extern` means a host or linker supplies the implementation; `export` exposes a function to a target environment.')],'export function add(a: i32, b: i32): i32 { return a + b }'),
d('control-flow','Language','Control flow','Use boolean conditions with straightforward if/else and while semantics.',[s('Boolean conditions','Integers do not silently become booleans. `if` and `while` require a boolean result.'),s('CFG','Structured flow lowers to explicit basic blocks and successor edges.')],'let i: int = 0\nwhile i < 10 {\n  if i == 5 { print(i) }\n  i = i + 1\n}'),
d('records','Language','Records','Group typed fields without turning simple data into an object hierarchy.',[s('Layout','Record fields retain declaration order and participate in native size/alignment calculation.'),s('Status','Record layout exists today. Rich constructors, pattern matching and tagged unions remain future language work rather than being documented as complete.')],'record Point { x: i32, y: i32 }'),
d('arrays-slices','Language','Arrays + slices','Distinguish owned fixed arrays from borrowed pointer-length slices.',[s('Ownership','`[T; N]` owns fixed storage. `[]T` is a borrowed view.'),s('Escape checking','The borrow checker rejects a returned slice when it is proven to point into a function-local fixed array.')],'let values: [u32; 3] = [1 as u32, 2 as u32, 3 as u32]'),
d('generics','Language','Constrained generics','Write one function, state useful constraints and let the compiler create concrete specializations.',[s('Constraints','1.0 supports generic capability names including Eq, Ord, Numeric, Integer and Copy.'),s('Monomorphization','Calls bind concrete argument types and reuse an instantiation cache so equivalent specializations are not emitted repeatedly.')],'function max<T: Ord>(a: T, b: T): T {\n  if a > b { return a }\n  return b\n}'),
d('modules','Language','Modules + imports','Split programs into real source units with canonical paths and source-aware diagnostics.',[s('Graph','Each imported file owns source text, AST and source ID. Imports resolve relative to their importing file.'),s('Current boundary','The graph is real; richer package-qualified namespace resolution continues to evolve.')],'module app.main\nimport "math.nqr"'),
d('errors','Language','Errors today','Use low-level integer status propagation where appropriate and explicit values elsewhere.',[s('Low-level status','`throw` and `try` support the current integer-status path used near ABI/system boundaries.'),s('Tagged results','Native `Option<T>` and `Result<T,E>` tagged unions are not yet complete in 1.0. Documentation does not pretend otherwise.')],'function raw(): isize {\n  throw 2\n}'),
d('memory-safety','Safety + systems','Memory safety model','Use safe application constructs by default and enter raw memory only when the task requires it.',[s('Protected paths','Static type checking, explicit integer conversions and slice escape checks eliminate several common classes of mistakes.'),s('Unsafe power remains','Raw pointers, inline assembly and volatile memory are still available. Noqeri therefore does not claim Rust-style global memory safety.'),s('Audit','`noqeri audit` makes high-risk capabilities visible during project review.')],'noqeri audit .'),
d('pointers','Safety + systems','Pointers','Keep raw addressing explicit and type-aware.',[s('Pointer types','`*T` and `*volatile T` expose raw addressing. Pointer compatibility is checked.'),s('Use deliberately','Prefer arrays, slices and host libraries in application code; pointers are intended for systems boundaries.')],'let value: u32 = 42\nlet ptr: *u32 = &value'),
d('volatile','Safety + systems','Volatile memory','Represent externally observed memory without confusing volatile with synchronization.',[s('Intent','Volatile is part of the pointer type and preserved through loads/stores.'),s('Concurrency','Volatile is not an atomic ordering primitive; use atomics for inter-thread coordination.')],'let status: *volatile u32 = 0xF0000000 as *volatile u32'),
d('atomics','Safety + systems','Atomics','Use explicit atomic load/store/exchange operations when shared-memory synchronization is actually needed.',[s('Operations','The bootstrap exposes atomic load, store, exchange, compare-exchange and fence operations.'),s('Reviewability','Atomic use remains syntactically visible and is surfaced by project audit tooling.')],'atomic.store(pointer, 1)\nlet old = atomic.exchange(pointer, 2)'),
d('intrinsics-asm','Safety + systems','Intrinsics + assembly','Reach target instructions without turning the whole language into target-specific syntax.',[s('Intrinsics','Known compiler/CPU intrinsics are checked before lowering.'),s('Assembly','Inline assembly is intentionally narrow and web output rejects it outright.')],'intrinsic("x86.pause")\nasm("nop")'),
d('nir','Safety + systems','Typed NIR','Inspect the compiler representation shared by the interpreter and machine backends.',[s('Values','NIR uses single-assignment virtual destination values with type metadata.'),s('Blocks','Functions expose basic blocks and successor edges, giving optimizers and allocators an explicit CFG.')],'noqeri nir tests/general_capabilities.nqr'),
d('optimization','Safety + systems','Optimization','Choose an optimization profile without changing program semantics.',[s('Profiles','Noqeri exposes O0, O1, O2, O3, Os and Oz.'),s('Pass manager','Optimization is organized as passes over NIR rather than hidden backend side effects.')],'noqeri build -O3 src/main.nqr build/main.s'),
d('register-allocation','Safety + systems','Register allocation','Understand the linear-scan allocation layer and its current backend boundary.',[s('Allocator','Noqeri computes live intervals, assigns physical registers and deterministic spill slots.'),s('Current status','The x86-64 backend still retains stack-backed fallback paths while allocation integration expands. This is not described as fully register-allocated codegen yet.')]),
d('targets','Safety + systems','Targets + ABI','Keep architecture/environment choice separate from language semantics.',[s('Targets','The default is `x86_64-unknown-none`; additional target contracts can exist before a complete machine backend.'),s('ABI','Host services are capabilities supplied through an ABI/runtime boundary instead of hard-coded syscalls.')],'noqeri targets'),
d('web-objects','Web + server','.nqo web objects','Compile checked Noqeri into an ES-module compatible artifact with a Noqeri-specific extension.',[s('Why .nqo','The extension identifies output as a Noqeri web object while retaining standard module semantics. Serve it as `text/javascript`.'),s('Portability','Browser helpers work directly; server-only capabilities call an injected NoqeriHost provider.')],'noqeri web src/client.nqr dist/client.nqo'),
d('browser-runtime','Web + server','Browser runtime','Use browser-safe helpers embedded into generated .nqo objects.',[s('Built-ins','The 1.0 portable runtime exposes JSON, URL, HTML escaping, routing, DOM mutations, fetch handles and WebSocket handles.'),s('Safety boundary','Raw memory, atomics, host/ABI calls, intrinsics and inline assembly are rejected by portable web lowering.')]),
d('dom','Web + server','DOM APIs','Change text, HTML, attributes and classes from Noqeri browser modules.',[s('Simple surface','DOM helpers use selectors rather than requiring a large object hierarchy in the language.'),s('Return values','Mutation helpers return bool so missing elements can be handled explicitly.')],'extern function domText(selector: string, value: string): bool\nexport function render(): bool { return domText("#app", "Hello") }'),
d('fetch','Web + server','Fetch API','Start network requests without adding async syntax before the language has a coherent async model.',[s('Handles','`fetchStart` returns a numeric handle. `fetchReady`, `fetchStatus`, `fetchOk`, and `fetchText` inspect state.'),s('Reason','The handle model keeps 1.0 semantics honest while future async/future syntax is designed.')],'let request: usize = fetchStart("/api/meta")\nwhile !fetchReady(request) { }\nprint(fetchText(request))'),
d('client-routing','Web + server','Client routing','Read and change browser history with a small portable surface.',[s('Path','`routePath()` returns the current pathname.'),s('Navigation','`routeGo(path)` pushes history and emits a popstate event so UI routers can react.')]),
d('json','Web + server','JSON','Validate, encode and decode JSON through the .nqo runtime.',[s('Browser/runtime','Generated .nqo objects map JSON helpers to the host JavaScript JSON implementation.'),s('Typed model','A rich Noqeri JSON value/tagged data model remains future work; 1.0 exposes a pragmatic codec surface first.')]),
d('url','Web + server','URL helpers','Encode and decode URL components without hand-written escaping.',[s('Portable','The browser runtime uses standard URI component semantics.'),s('Server','Server hosts can expose equivalent behavior without changing Noqeri source.')]),
d('html','Web + server','HTML helpers','Escape untrusted text before interpolation into HTML strings.',[s('Escape','`htmlEscape` covers ampersand, angle brackets, quotes and apostrophes.'),s('DOM preference','Use `domText` when plain text is intended; it avoids HTML interpretation entirely.')]),
d('http','Web + server','HTTP model','Keep transport in the host and application decisions in Noqeri.',[s('Separation','Node, native hosts or future runtimes own sockets and HTTP parsing. Noqeri code owns status/body/routing decisions through exported functions or the http package.'),s('Why','This avoids making Node or one operating system part of the language semantics.')]),
d('http-server','Web + server','HTTP server','Run Noqeri-authored API logic behind a production host transport.',[s('Current web host','The official website imports a generated backend `.nqo`, calls its exported route functions for API requests and uses Node only for transport/static-file responsibilities.'),s('Capabilities','The http-server package exposes route/static/listen capabilities for hosts that implement them.')]),
d('static-files','Web + server','Static files','Serve built frontend assets with traversal protection, MIME types, gzip and cache policy.',[s('Official host','The website host resolves requested files inside its build root, rejects path escapes and uses immutable caching for fingerprinted assets.'),s('SPA fallback','Unknown application routes fall back to index.html so `/docs/*` works with client routing.')]),
d('websocket','Web + server','WebSocket','Use numeric handles for browser WebSocket connections.',[s('Operations','Open, send, receive queued text messages and close.'),s('Server status','Browser support exists in the portable runtime. A production native/server WebSocket provider remains an ecosystem task.')]),
d('server-host','Web + server','NoqeriHost','Understand the capability provider behind server-only .nqo calls.',[s('Injection','Generated .nqo objects ask `globalThis.NoqeriHost` only when a server-only capability is invoked.'),s('Security','The official host restricts filesystem access to its data directory and environment access to an allowlist; crypto delegates to Node crypto rather than home-grown algorithms.')]),
d('nqd','Data + NoqeriDB','.nqd language','Use a smaller typed data language for embedded NoqeriDB work.',[s('Commands','1.0 supports database selection, table declarations, insert, select, update and delete.'),s('Types','Columns use int, real, bool and text plus required, unique and key constraints.')],'database "app.nqdb"\ntable users { id: int key, name: text required }\ninsert users { id: 1, name: "Ada" }\nselect users where id = 1'),
d('noqeridb','Data + NoqeriDB','NoqeriDB','Use the embedded database engine without SQL ceremony.',[s('Engine','NoqeriDB is implemented in the compiler/runtime repository and exposed through `noqeri db`.'),s('Honest scope','The 1.0 engine is a typed embedded row store. It is not claimed to outperform or replace SQLite until durability, concurrency and benchmark evidence justify that comparison.')]),
d('database-schema','Data + NoqeriDB','Schemas + constraints','Put common integrity rules next to the table shape.',[s('Constraints','`required`, `unique` and `key` are validated before a transaction is committed.'),s('Type checking','Inserted/updated values must match the declared column type.')],'table accounts {\n  id: int key,\n  email: text required unique,\n  active: bool required\n}'),
d('database-queries','Data + NoqeriDB','Queries','Read and mutate rows with a deliberately small vocabulary.',[s('Read','`select table` returns tabular output; add `where column = value` for equality filtering.'),s('Mutate','`insert`, `update ... set`, and `delete` use object-like field syntax instead of SQL value lists.')]),
d('database-transactions','Data + NoqeriDB','Transactions + durability','Treat each .nqd script as one commit unit.',[s('Atomic script','The engine loads state, validates every statement, and replaces the durable file only after the full script succeeds.'),s('Current limitation','Multi-process locking, WAL mode, indexes and concurrent MVCC are not yet implemented; they are required before calling NoqeriDB an advanced SQLite competitor.')]),
d('database-web','Data + NoqeriDB','Database-backed web API','Use NoqeriDB from a server .nqo host without turning the database into JavaScript application code.',[s('Official site','The server host maps dbOpen/dbExec to the actual `noqeri db` executable with argv-based subprocess execution and a restricted data directory.'),s('End-to-end test','CI exercises `/api/db-demo`, which travels through Noqeri-authored routing into a .nqd transaction and back into the HTTP response.')]),
d('postgres','Data + NoqeriDB','PostgreSQL provider','Target external PostgreSQL through an official package surface.',[s('Status','The 1.0 package defines the provider contract but is marked experimental until the wire/client provider and integration tests exist.'),s('No fake production claim','Compilation of declarations is not treated as evidence that PostgreSQL networking works.')]),
d('sqlite','Data + NoqeriDB','SQLite interoperability','Keep a path for SQLite projects while NoqeriDB evolves.',[s('Status','The official package currently defines a provider surface and is experimental.'),s('Relationship','NoqeriDB is a separate database language/engine, not a renamed SQLite wrapper.')]),
d('registry','Packages + security','Registry','Discover package metadata and download deterministic archives.',[s('Website URL','The official web host exposes `/registry/index.json` and `/registry/{namespace}/{name}/{version}/download.nqpkg`.'),s('Build gate','Registry CI builds the compiler and type-checks every published 1.0 entrypoint.')]),
d('package-manifest','Packages + security','Package manifests','Describe library identity with package.nqr.',[s('Fields','Package manifests carry name, version, edition, entry, profile and target.'),s('Simple by design','The format follows the same readable manifest grammar as projects.')]),
d('lock-files','Packages + security','Lock files','Use content identity rather than mutable version labels alone.',[s('SHA-256','Lock v2 uses SHA-256 identities. Local path dependencies are hashed deterministically.'),s('Remote packages','A remote dependency without immutable checksum metadata is rejected during locking.')]),
d('library-catalog','Packages + security','Library catalog','Track a 200-library roadmap without publishing empty promises as finished packages.',[s('Roadmap','The ecosystem roadmap spans core, memory, text, math, I/O, concurrency, networking, web, serialization, databases, security, GUI, science, AI and cloud.'),s('Promotion rule','A library becomes production only after implementation plus compile/runtime/integration testing; placeholder names do not improve the library score.')]),
d('security','Packages + security','Security package','Use bounded validation and explicit memory clearing at low-level boundaries.',[s('Current package','The security package provides volatile memory clearing and overflow-resistant range/index checks.'),s('Crypto boundary','Cryptographic algorithms use trusted host/provider implementations rather than hand-written algorithms added merely to increase package count.')]),
d('crypto','Packages + security','Crypto providers','Request secure random bytes and SHA-256 from a trusted host.',[s('Website host','The Node provider maps these calls to `node:crypto`.'),s('Native future','Native providers need platform entropy/TLS libraries and dedicated test vectors before promotion.')]),
d('audit','Packages + security','Security audit','Review both dependency integrity and explicit high-risk capabilities.',[s('Integrity','Audit checks lock-v2 presence and SHA-256 dependency identities.'),s('Capabilities','Inline assembly, intrinsics, volatile memory, host/ABI calls and pointer casts are surfaced as review points rather than automatically labelled vulnerabilities.')],'noqeri audit .'),
d('testing','Tooling + deployment','Testing','Use positive behavior tests, invalid-program tests, sanitizer builds and platform CI together.',[s('Compiler CI','Linux, macOS and Windows builds plus sanitizer/native jobs protect the language implementation.'),s('Ecosystem CI','Registry CI compiles published packages; website CI regenerates .nqo artifacts, builds the frontend, runs API/DB/registry smoke tests and builds Docker.')]),
d('formatting','Tooling + deployment','Formatter','Format source without losing comments.',[s('Trivia','The lexer has a trivia-preserving mode for line/block comments.'),s('Regression','Formatter tests assert that comments survive formatting.')],'noqeri format src/main.nqr'),
d('lsp','Tooling + deployment','Language server','Compile unsaved editor text against the real module graph.',[s('Overlays','Open-document text overlays the root file while imported modules continue to resolve from disk.'),s('Current features','Diagnostics and formatting are implemented; completion/rename/reference features remain future tooling work.')],'noqeri lsp'),
d('deployment','Tooling + deployment','Deploy web applications','Build Noqeri .nqo objects before Vite and keep the runtime capability provider separate.',[s('Docker','The official site Docker build compiles Noqeri, regenerates server/client .nqo objects, builds the frontend and ships the compiler for NoqeriDB queries.'),s('Production boundary','Transport, TLS termination and durable volume configuration remain deployment responsibilities.')]),
d('performance','Tooling + deployment','Performance','Benchmark compiler throughput and database/runtime behavior before making comparative claims.',[s('Compiler','Existing compiler benchmarks measure check/run/build throughput, not generated-program performance.'),s('Database','NoqeriDB needs durable benchmark suites against SQLite and realistic workloads before any “faster than SQLite” claim is valid.')]),
d('roadmap','Tooling + deployment','1.0 → next','Understand what makes the language mid-tier rather than merely adding names.',[s('Next language work','Tagged unions/Option/Result, richer owned collections, async model, stronger safe profile, namespace-qualified package resolution and deeper native allocation integration.'),s('Next ecosystem work','Promote roadmap libraries in tested layers: JSON value model, native HTTP/TLS, database indexes/WAL/concurrency, filesystem/process, collections/text, and real external DB drivers.')])
]
