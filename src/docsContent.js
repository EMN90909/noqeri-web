export const docGroups=['Start','Language','Memory + systems','Projects + tooling','Web + security']

const s=(title,paragraphs,points)=>({title,paragraphs:Array.isArray(paragraphs)?paragraphs:[paragraphs],points})
export const docs=[
{slug:'getting-started',group:'Start',title:'Getting started',summary:'Build, check and run a first Noqeri program and understand the project loop.',sections:[s('First program','Source files use the .nqr extension. The normal development loop is check first, run through the reference runtime when portability matters, then build for an explicit target when you need a native artifact.', ['Use `noqeri check` as the fast semantic gate.','Use `noqeri run` for reference execution.','Use `noqeri build` for target code generation.']),s('Project shape','A project is anchored by project.nqr and noqeri.lock. Source can be split into modules with explicit imports; packages remain separately content-addressed.')],code:'function main(): int {\n    print("hello, noqeri")\n    return 0\n}'},
{slug:'installation',group:'Start',title:'Installation',summary:'Build the compiler, inspect the version contract and verify the toolchain.',sections:[s('Bootstrap','Noqeri currently bootstraps through a C++17 compiler implementation. CMake produces the `noqeri` CLI and public ABI library on Linux, macOS and Windows.'),s('Verify','Run the version and doctor commands after installation. The doctor checks the repository/toolchain contract rather than assuming one operating system.')],code:'cmake -S . -B build -DCMAKE_BUILD_TYPE=Release\ncmake --build build --parallel\n./build/noqeri version\n./build/noqeri doctor .'},
{slug:'project-manifest',group:'Start',title:'project.nqr manifest',summary:'Declare package identity, edition, target, registry and dependencies with a real grammar.',sections:[s('Manifest','The manifest is parsed structurally rather than with regular expressions. Core fields define project identity and build defaults; dependencies may resolve from registry, Git or local paths.'),s('Determinism','The companion lock file records immutable SHA-256 content identities. A remote dependency without an immutable identity is not considered locked.')],code:'project {\n    name: "sample"\n    version: "0.1.0"\n    edition: "2026"\n    target: "x86_64-unknown-none"\n    registry: "https://registry.example"\n}\n\ndependencies {\n    core: "1.5.0"\n}'},
{slug:'cli',group:'Start',title:'CLI reference',summary:'The compiler, project, target, formatter, package and language-server commands.',sections:[s('Core commands','The CLI intentionally exposes compiler stages instead of hiding them.', ['check — parse, type-check and safety-check','nir — print typed intermediate representation','run — reference execution','build — emit target assembly/code','assemble / link — explicit external tool adapters','web — emit an ES module for browser or server hosts']),s('Project commands','`manifest`, `lock`, `targets`, `format`, `test`, `doctor`, `audit` and `lsp` cover project/tooling workflows.')],code:'noqeri check src/main.nqr\nnoqeri build -O2 --target x86_64-unknown-none src/main.nqr build/main.s\nnoqeri web src/ui.nqr dist/ui.mjs'},
{slug:'language-tour',group:'Start',title:'Language tour',summary:'A compact tour through declarations, functions, records, arrays, slices and explicit systems features.',sections:[s('Readable by default','Noqeri uses `let`, `const`, `function`, `record`, `if`, `while`, `return` and conventional operators. Semicolons are optional at statement boundaries.'),s('Explicit capability','Pointers, volatile access, atomics, target intrinsics and inline assembly are visible in source. The language does not hide these behind an unrelated “kernel mode”.')],code:'record Point { x: i32, y: i32 }\n\nfunction length2(p: Point): i32 {\n    return p.x * p.x + p.y * p.y\n}'},
{slug:'types',group:'Language',title:'Type system',summary:'Static scalar, pointer, aggregate, borrowed and tagged-result types.',sections:[s('Scalar types','Integers are available at fixed widths together with `usize`, `isize`, `int`, `float`, `bool`, `string`, `null` and `void`. Type checking happens before lowering.'),s('Conversion rule','Safe widening is implicit. Narrowing and signedness-changing conversions require `as`; fixed-width literals are accepted only when the compiler can prove they fit.')],code:'let port: u16 = 8080\nlet small: u8 = 42\nlet wide: u32 = small\nlet clipped: u8 = wide as u8'},
{slug:'variables',group:'Language',title:'Variables + constants',summary:'Local bindings, inference, annotations and immutable constants.',sections:[s('Bindings','`let` creates a mutable binding and `const` creates a binding that cannot be reassigned. An annotation constrains the initializer; without one, the compiler infers the expression type.'),s('Scope','Bindings are lexical. Duplicate symbols in the same scope are rejected and inner blocks may create independent bindings.')],code:'const limit: u32 = 64\nlet count: u32 = 0\ncount = count + 1'},
{slug:'functions',group:'Language',title:'Functions',summary:'Typed parameters, return values, export/extern boundaries and call checking.',sections:[s('Signatures','Functions declare parameter types and an optional return type. Calls are checked for arity and assignability before NIR generation.'),s('Boundaries','`export` exposes a function through a target ABI symbol. `extern` declares a function supplied by the host or linker; these are distinct from ordinary module calls.')],code:'export function add(left: i32, right: i32): i32 {\n    return left + right\n}\n\nextern function host_log(value: string): void'},
{slug:'control-flow',group:'Language',title:'Control flow',summary:'Boolean conditions, if/else, while loops and explicit returns.',sections:[s('Conditions','`if` and `while` require `bool`; integers do not silently become truth values. This keeps branch intent unambiguous.'),s('Lowering','Structured control flow lowers into explicit basic blocks and conditional/unconditional edges in NIR.')],code:'let i: u32 = 0\nwhile i < 10 {\n    if i == 5 { print(i) }\n    i = i + 1\n}'},
{slug:'records',group:'Language',title:'Records',summary:'Declaration-order aggregate layout with field offsets and natural alignment.',sections:[s('Layout','Record fields retain declaration order. The checker computes size, alignment and offsets; incomplete recursive values are rejected and should use pointers.'),s('Access','Field access works on values and pointer-to-record bases. Native lowering turns fields into explicit pointer offsets.')],code:'record Header {\n    tag: u32\n    length: usize\n}\nlet h: Header = null as Header // illustrative layout only'},
{slug:'arrays-slices',group:'Language',title:'Arrays + slices',summary:'Owned fixed-size values and borrowed pointer-length spans with lifetime checks.',sections:[s('Ownership','`[T; N]` is an owned fixed-size value. `[]T` is a borrowed span represented by data pointer plus length.'),s('Lifetime','Returning a slice proven to originate from a local fixed array is rejected. Use caller-owned storage or a future owned container when data must outlive the local frame.')],code:'function view(data: *u32, count: usize): []u32 {\n    return slice(data, count)\n}'},
{slug:'generics',group:'Language',title:'Constrained generics',summary:'Inferred type arguments, constraints and per-type monomorphized function instances.',sections:[s('Constraints','Generic parameters can name capabilities such as `Eq`, `Ord`, `Numeric`, `Integer` and `Copy`. A call that binds a type without the required capability is rejected before lowering.'),s('Monomorphization','Each concrete generic binding is assigned a stable mangled instance and lowered as a separate function. The instantiation cache prevents duplicate specializations within a compilation.')],code:'function max<T: Ord>(a: T, b: T): T {\n    if a > b { return a }\n    return b\n}\n\nprint(max(10, 4))'},
{slug:'option-result',group:'Language',title:'Option + Result',summary:'Tagged core types for absence and typed success/failure without exceptions.',sections:[s('Option','`Option<T>` models a value that may be absent. Core constructors and predicates use an explicit tag so absence is not represented by a magic payload.'),s('Result','`Result<T,E>` separates a success payload from an error payload. `try` propagates a typed Result failure when the enclosing function returns a compatible Result; integer-status `try` remains available for raw low-level APIs.')],code:'function find(id: u64): Option<u64> {\n    if id == 0 { return none<u64>() }\n    return some(id)\n}'},
{slug:'errors',group:'Language',title:'Error handling',summary:'Low-level status propagation and application-level tagged Result values.',sections:[s('Raw status','Freestanding or ABI-near code can use integer-returning functions with `throw` and `try`. Negative values represent propagated failure status.'),s('Typed application errors','Application libraries should prefer `Result<T,E>` so callers retain the error type rather than collapsing every failure into an integer.')],code:'function rawWrite(): isize {\n    throw 2\n}\nfunction start(): isize {\n    try rawWrite()\n    return 0\n}'},
{slug:'pointers-memory',group:'Memory + systems',title:'Pointers + memory',summary:'Address-of, dereference, pointer arithmetic and visible raw-memory operations.',sections:[s('Pointer contract','`*T` is an explicit raw pointer. `&value` produces an address and `*ptr` dereferences it. Pointer conversions are checked and incompatible pointee assignments do not silently pass.'),s('Safety boundary','Noqeri is strongly statically typed, not memory-safe in the Rust sense. Raw pointers and inline assembly deliberately permit operations whose validity depends on the programmer and ABI contract.')],code:'let value: u32 = 42\nlet ptr: *u32 = &value\n*ptr = 43'},
{slug:'volatile',group:'Memory + systems',title:'Volatile access',summary:'Make device- or externally-observable memory operations explicit.',sections:[s('Type','`*volatile T` carries volatile access intent in the pointer type. Loads/stores preserve the qualifier through lowering.'),s('Use','Volatile is for externally observed memory such as MMIO. It is not a synchronization primitive; use atomics for inter-thread ordering.')],code:'let status: *volatile u32 = 0xF0000000 as *volatile u32\nlet current: u32 = *status'},
{slug:'atomics',group:'Memory + systems',title:'Atomics',summary:'Load, store, exchange, compare-exchange and fence operations for scalar/pointer values.',sections:[s('Operations','Atomic operations accept bool, integer or pointer values up to the native supported width. The bootstrap uses sequentially consistent ordering by default.'),s('Native lowering','x86-64 lowering uses hardware atomic instructions and explicit fences where required. Future memory-order syntax can refine this without changing the core operation model.')],code:'let counter: u64 = 0\nlet p: *u64 = &counter\natomic.store(p, 1)\nlet old: u64 = atomic.exchange(p, 2)'},
{slug:'intrinsics-asm',group:'Memory + systems',title:'Intrinsics + inline asm',summary:'Target intrinsics and deliberately constrained one-instruction assembly.',sections:[s('Intrinsics','Named intrinsics expose target/compiler operations such as pause, timestamp counters and compiler fences. Unsupported names fail during checking.'),s('Assembly','`asm` currently accepts one instruction and rejects directives, labels and embedded newlines. This keeps bootstrap inline assembly narrow and auditable.')],code:'intrinsic("x86.pause")\nlet ticks: u64 = intrinsic("x86.rdtsc")\nasm("nop")'},
{slug:'abi-host',group:'Memory + systems',title:'ABI + host services',summary:'Keep environment services behind an explicit ABI rather than baking an OS into the language.',sections:[s('ABI','Generated native code receives a Noqeri ABI table from its embedding host. Output, clocks and platform naming are optional services rather than language syscalls.'),s('Portability','A target adapter can bridge the Noqeri ABI to Linux, Windows, firmware, a game host or another runtime without changing source semantics.')],code:'export function start(): isize {\n    print(platform())\n    return 0\n}'},
{slug:'targets',group:'Memory + systems',title:'Targets',summary:'Explicit architecture-vendor-environment triples and portable frontend semantics.',sections:[s('Triples','Targets describe architecture and execution environment independently. The default freestanding x86-64 target is `x86_64-unknown-none`.'),s('Adapters','Assembler and linker adapters are argv-based subprocesses. The compiler does not construct shell command strings.')],code:'noqeri targets\nnoqeri build --target x86_64-unknown-none src/main.nqr build/main.s'},
{slug:'optimization',group:'Memory + systems',title:'Optimization',summary:'A pass manager with explicit optimization profiles and independently testable transforms.',sections:[s('Profiles','`-O0`, `-O1`, `-O2`, `-O3`, `-Os` and `-Oz` select pass pipelines rather than scattering optimization decisions through code generation.'),s('Passes','Canonicalization, propagation/folding, dead-code cleanup and CFG-oriented passes operate on NIR. More aggressive passes can be added without changing parsing or type checking.')],code:'noqeri build -O3 src/main.nqr build/main.s\nnoqeri build -Oz src/main.nqr build/small.s'},
{slug:'nir',group:'Memory + systems',title:'Typed NIR + CFG',summary:'Single-assignment virtual values, typed instructions and explicit basic-block control flow.',sections:[s('Representation','NIR sits between semantic analysis and backends. Virtual destination registers are single-assignment values carrying types; functions expose basic blocks and successor edges.'),s('Backend contract','Native, interpreter and web backends consume the same checked program contract while remaining free to choose their own machine/runtime representation.')],code:'noqeri nir tests/general_capabilities.nqr'},
{slug:'modules-imports',group:'Projects + tooling',title:'Modules + imports',summary:'A real source graph with canonical paths, per-file source IDs and cycle-safe loading.',sections:[s('Graph','Imports resolve relative to their importing module. Each file owns its source text and AST; the compiler no longer concatenates source files before diagnostics.'),s('Names','`module` records logical organization. Imported definitions enter the compilation graph; namespace-qualified resolution is designed to remain explicit as the module system expands.')],code:'module app.main\nimport "math.nqr"\n\nprint(add(2, 3))'},
{slug:'packages-registry',group:'Projects + tooling',title:'Packages + registry',summary:'Registry, Git and path dependencies backed by immutable package identities.',sections:[s('Registry','The official registry protocol exposes package metadata and deterministic `.nqpkg` archives. Packages are addressed by namespace/name/version plus SHA-256 content identity.'),s('Resolver','The package resolver checks lock identities before exposing package source to the module graph. Local/path dependencies are hashed directly; remote packages must resolve to immutable content.')],code:'noqeri manifest project.nqr\nnoqeri lock project.nqr\nnoqeri audit .'},
{slug:'testing',group:'Projects + tooling',title:'Testing',summary:'Directory test execution, semantic fixtures, negative type tests and native validation.',sections:[s('Language tests','`noqeri test` compiles and runs `.nqr` fixtures. CI also keeps invalid programs that must remain rejected.'),s('Compiler tests','The project exercises Linux, macOS and Windows builds, sanitizers, emitted assembly, real assembler acceptance and repeatable compiler throughput benchmarks.')],code:'noqeri test tests\n./scripts/test.sh\n./scripts/benchmark.sh'},
{slug:'formatting',group:'Projects + tooling',title:'Formatting',summary:'Canonical formatting that preserves line and block comments as lexer trivia.',sections:[s('Trivia','The lexer can preserve whitespace-adjacent comments as trivia tokens for source tools while the compiler parser continues to consume only significant syntax.'),s('Invariant','`noqeri format` must never erase programmer comments. This is regression-tested before formatting becomes a default editor action.')],code:'noqeri format src/main.nqr'},
{slug:'lsp',group:'Projects + tooling',title:'Language server',summary:'Project-aware diagnostics, formatting and a workspace path toward navigation/refactoring.',sections:[s('Workspace','The server maintains open-document overlays while resolving imports through the same ModuleGraph used by the compiler. Diagnostics retain their real source file identity.'),s('Protocol','Compiler version information comes from the canonical version API. Diagnostics and formatting are the baseline; completion, hover, definitions, references, rename and semantic tokens build on the same workspace graph.')],code:'noqeri lsp'},
{slug:'security-audit',group:'Projects + tooling',title:'Security audit',summary:'Audit dependency integrity and visible high-risk language capabilities.',sections:[s('Project audit','`noqeri audit` checks the manifest/lock relationship, SHA-256 identities and duplicate/unlocked dependency conditions.'),s('Source audit','The audit also reports raw pointers, inline assembly, volatile memory and host-service calls as review points. These are capabilities, not automatic vulnerabilities, so findings are explicit rather than silently blocked.')],code:'noqeri audit .'},
{slug:'web-frontend',group:'Web + security',title:'Browser frontend',summary:'Compile checked Noqeri modules into small ES modules for browser integration.',sections:[s('Web emitter','`noqeri web` emits an ES module from the same checked AST. Exported Noqeri functions become named JavaScript exports and ordinary pure functions remain local.'),s('Host boundary','DOM access remains a host concern. A browser adapter can call exported Noqeri functions from React, vanilla JS or another framework without giving the language implicit browser globals.')],code:'export function headline(name: string): string {\n    return "Hello " + name\n}\n\n// noqeri web ui.nqr ui.mjs'},
{slug:'web-backend',group:'Web + security',title:'Web backend',summary:'Run Noqeri-authored application logic behind a small HTTP host adapter.',sections:[s('Separation','HTTP sockets, TLS and process lifecycle belong to a host adapter. Route/business functions can be authored in Noqeri and emitted as an ES module for a Node host today.'),s('Deployment','The official website uses this pattern: backend API values come from `backend/api.nqr`; a hardened Node adapter handles transport and static assets. The language remains portable to future native HTTP adapters.')],code:'export function apiStatus(): string { return "ok" }\nexport function apiEdition(): string { return "2026" }'},
{slug:'web-security',group:'Web + security',title:'Web security',summary:'Secure defaults for generated modules, server adapters and dependency boundaries.',sections:[s('Generated code','The web emitter does not use `eval`, `Function` constructors or dynamic code loading. Exports are static and source maps remain optional.'),s('Server adapter','Deployments should set content-type hardening, CSP, referrer/permissions policies, path traversal protection, immutable asset caching and strict request-size/time limits.')],code:'// review with\nnoqeri audit .\n// then run project and integration tests'},
{slug:'security-library',group:'Web + security',title:'Security library',summary:'Constant-time helpers, memory clearing and validation primitives without pretending to be a full cryptography suite.',sections:[s('Scope','The standard security package provides primitives that can be implemented safely with current Noqeri semantics: constant-time byte comparison, explicit zeroing and bounded validation helpers.'),s('Cryptography','High-level cryptographic algorithms and OS entropy should come from reviewed platform/crypto providers through explicit adapters rather than ad-hoc implementations in application code.')],code:'import "security.nqr"\nlet same: bool = constantTimeEqual(left, right)'},
{slug:'deployment',group:'Web + security',title:'Deployment',summary:'Build once, serve static frontend assets and run a small host adapter around Noqeri web modules.',sections:[s('Container','The reference web deployment uses a multi-stage Node image: Vite builds static assets, then a minimal Node runtime serves those assets and the generated Noqeri API module.'),s('Health','Expose a cheap `/api/health` endpoint, run it as the container health check and keep the service stateless so platform restarts remain safe.')],code:'docker build -t noqeri-web .\ndocker run -p 8080:8080 noqeri-web'},
{slug:'performance',group:'Web + security',title:'Performance + benchmarking',summary:'Measure compiler throughput separately from generated-program runtime and web delivery.',sections:[s('Compiler','The repository benchmark measures check, reference run and code-generation throughput over a generated multi-function source file. It is not presented as a C/Rust/Zig runtime comparison.'),s('Web','Frontend performance favors static route content, CSS transitions over animation frameworks, cacheable hashed assets and a tiny transport adapter.')],code:'./scripts/benchmark.sh'},
{slug:'compatibility',group:'Web + security',title:'Compatibility contract',summary:'Language edition, compiler version, ABI version and package protocol evolve independently.',sections:[s('Versions','A compiler can support more than one language edition and target ABI. Keeping these values separate prevents an implementation release from silently redefining source semantics.'),s('Stability','Public headers and registry metadata expose canonical version constants. Tooling reads those values rather than duplicating stale version strings.')],code:'noqeri version'}
]
