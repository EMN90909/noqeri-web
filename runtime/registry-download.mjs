import { gzipSync } from 'node:zlib'

const INDEX='https://raw.githubusercontent.com/EMN90909/noqeri-registry/main/registry/index.json'
const API='https://api.github.com/repos/EMN90909/noqeri-registry/contents/'
const cache=new Map()
const validPart=value=>/^[a-z0-9][a-z0-9-]*$/i.test(value)
const oct=(value,length)=>`${Math.max(0,value).toString(8)}`.padStart(length-1,'0')+'\0'
function tarHeader(name,size){const h=Buffer.alloc(512);h.write(name,0,100,'utf8');h.write(oct(0o644,8),100,8,'ascii');h.write(oct(0,8),108,8,'ascii');h.write(oct(0,8),116,8,'ascii');h.write(oct(size,12),124,12,'ascii');h.write(oct(0,12),136,12,'ascii');h.fill(0x20,148,156);h[156]='0'.charCodeAt(0);h.write('ustar\0',257,6,'ascii');h.write('00',263,2,'ascii');let sum=0;for(const b of h)sum+=b;h.write(oct(sum,8),148,8,'ascii');return h}
function tar(entries){const chunks=[];for(const entry of entries){const data=Buffer.from(entry.data);chunks.push(tarHeader(entry.name,data.length),data);const pad=(512-data.length%512)%512;if(pad)chunks.push(Buffer.alloc(pad))}chunks.push(Buffer.alloc(1024));return Buffer.concat(chunks)}
async function getJson(url){const response=await fetch(url,{headers:{'Accept':'application/vnd.github+json','User-Agent':'noqeri-web'},signal:AbortSignal.timeout(8000)});if(!response.ok)throw new Error(`registry fetch ${response.status}`);return response.json()}
async function listFiles(path,base,files){const listing=await getJson(`${API}${path}?ref=main`);for(const item of listing){if(item.type==='dir')await listFiles(item.path,base,files);else if(item.type==='file'){if(files.length>=100)throw new Error('package file limit exceeded');const response=await fetch(item.download_url,{signal:AbortSignal.timeout(8000)});if(!response.ok)throw new Error('package file fetch failed');const data=Buffer.from(await response.arrayBuffer());files.push({name:item.path.slice(base.length+1),data})}}
export async function registryIndex(){const now=Date.now(),hit=cache.get('index');if(hit&&now-hit.time<300000)return hit.value;const value=await getJson(INDEX);cache.set('index',{time:now,value});return value}
export async function packageArchive(namespace,name,version){if(![namespace,name,version].every(validPart))throw new Error('invalid package identity');const key=`${namespace}/${name}@${version}`,hit=cache.get(key);if(hit&&Date.now()-hit.time<300000)return hit.value;const index=await registryIndex();const pkg=index.packages.find(p=>p.namespace===namespace&&p.name===name);const release=pkg?.versions?.find(v=>v.version===version&&!v.yanked);if(!release)throw new Error('package not found');const files=[];await listFiles(release.path,release.path,files);let total=0;for(const f of files)total+=f.data.length;if(total>5*1024*1024)throw new Error('package size limit exceeded');const value=gzipSync(tar(files),{level:9,mtime:0});cache.set(key,{time:Date.now(),value});return value}
